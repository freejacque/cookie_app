<header>
  <div class="home"><span><%= link_to 'Home', root_path %></span></div>
  <div class="profile">
    <span>Welcome <%= @user.name %>! (<%= link_to("Logout", session_path, method: :delete) %>)</span><br/>
    <span><%= link_to 'Edit your profile', edit_user_path(@user) %></span>
  </div>
  <hr/>
</header>
